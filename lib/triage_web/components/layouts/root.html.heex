<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title default="Triage" suffix=" · Email Sorting">
      {assigns[:page_title]}
    </.live_title>
    <link rel="icon" href={~p"/favicon.ico"} sizes="any" />
    <link rel="icon" type="image/svg+xml" href={~p"/favicon.svg"} />
    <link phx-track-static rel="stylesheet" href={~p"/assets/css/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/js/app.js"}>
    </script>
  </head>
  <body class="min-h-screen bg-gradient-to-b from-white via-slate-50 to-slate-100 text-slate-900 antialiased">
    <header class="md:sticky top-0 z-50 border-b border-slate-200 bg-white/95 shadow-lg shadow-slate-200/50">
      <nav class="mx-auto flex max-w-6xl items-center justify-between px-4 py-4 sm:px-6 lg:px-8">
        <.link href={~p"/"} class="flex items-center gap-3 text-slate-900">
          <span class="inline-flex h-11 w-11 items-center justify-center rounded-2xl bg-gradient-to-br from-sky-500 to-indigo-500 text-lg font-semibold text-white shadow-lg shadow-sky-500/30">
            AI
          </span>
          <div>
            <p class="text-base font-semibold tracking-tight">Triage</p>
            <p class="text-sm text-slate-500">Email intelligence</p>
          </div>
        </.link>

        <div class="hidden items-center gap-6 text-sm font-medium text-slate-500 md:flex">
          <%= if @current_scope do %>
            <.link
              href={~p"/"}
              class="transition hover:text-slate-900"
            >
              Home
            </.link>
            <.link
              href={~p"/emails"}
              class="transition hover:text-slate-900"
            >
              Inbox
            </.link>
            <.link
              href={~p"/email_accounts"}
              class="transition hover:text-slate-900"
            >
              Accounts
            </.link>
            <.link
              href={~p"/email_rules"}
              class="transition hover:text-slate-900"
            >
              Rules
            </.link>
          <% else %>
            <a href="/#hero" class="transition hover:text-slate-900">Product</a>
            <a href="/#features" class="transition hover:text-slate-900">Features</a>
            <a href="/#workflow" class="transition hover:text-slate-900">Workflow</a>
          <% end %>
        </div>

        <div class="flex items-center gap-3">
          <%= if @current_scope do %>
            <div class="hidden flex-col text-right text-xs text-slate-500 sm:flex">
              <span class="font-semibold text-slate-900">{@current_scope.user.email}</span>
            </div>
            <.link
              href={~p"/users/log-out"}
              method="delete"
              class="rounded-full bg-gradient-to-r from-sky-500 to-blue-600 px-4 py-2 text-sm font-semibold text-white shadow-lg shadow-sky-500/30 transition hover:-translate-y-0.5"
            >
              Log out
            </.link>
          <% else %>
            <.link
              href={~p"/users/oauth/google/request"}
              class="rounded-full bg-gradient-to-r from-sky-500 to-blue-600 px-4 py-2 text-sm font-semibold text-white shadow-lg shadow-sky-500/30 transition hover:-translate-y-0.5"
            >
              Sign in with Google
            </.link>
          <% end %>
        </div>
      </nav>
    </header>

    <div class="pb-20">
      {@inner_content}
    </div>
  </body>
</html>
